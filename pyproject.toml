[project]
name = "boilerplate-backend"
version = "1.1.0"
description = "AI-Powered SaaS Boilerplate Backend"
authors = [{ name = "Johan Guse", email = "johanguse@gmail.com" }]
readme = "README.md"
requires-python = ">=3.11,<3.13"

dependencies = [
  "fastapi[standard]>=0.141.1",
  "fastapi-users[oauth,sqlalchemy]>=15.0.5",
  "fastapi-pagination[sqlalchemy]>=0.15.12",
  "sqlalchemy>=2.0.49",
  "psycopg2-binary>=2.9.12",
  "pydantic[email]>=2.13.3",
  "alembic>=1.19.2",
  "uvicorn>=0.52.4",
  "pyjwt[crypto]>=2.12.1",
  "passlib[argon2]>=1.7.4",
  "bcrypt>=4.3.0,<5.0.0",
  "python-dotenv>=1.2.2",
  "pydantic-settings>=2.14.2",
  "python-multipart>=0.0.31",
  "pandas>=3.0.2",
  "chardet>=7.4.3",
  "celery>=5.6.3",
  "resend>=2.29.0",
  "asyncpg>=0.31.0",
  "boto3>=1.43.89",
  "httpx-oauth>=0.17.0",
  "sqlalchemy-utils>=0.42.1",
  "loguru>=0.7.3",
  "python-json-logger>=4.1.0",
  "ua-parser>=1.0.2",
  "user-agents>=2.2.0",
  "python-terraform>=0.10.1",
  "cachetools>=7.0.6",
  "stripe>=15.6.1",
  "cryptography>=48.0.1",
  "python-magic>=0.4.27",
  "babel>=2.18.0",
  "uvloop>=0.22.1",
  "httptools>=0.7.1",
  "orjson>=3.11.8",
  "slowapi>=0.1.9",
  "openai>=2.32.0",
  "anthropic>=0.96.0",
  "tiktoken>=0.12.0",
  "pypdf>=6.14.2",
  "python-docx>=1.2.0",
  "langchain-core>=1.3.3",
]

[dependency-groups]
dev = [
  "pytest>=9.0.3",
  "pytest-cov>=7.1.0",
  "ruff>=0.15.11",
  "httpx>=0.28.1",
  "testcontainers>=4.14.2",
  "factory-boy>=3.3.3",
  "pytest-asyncio>=1.3.0",
  "aiosqlite>=0.22.1",
]

[tool.ruff]
line-length = 79
extend-exclude = ['migrations']

[tool.ruff.lint]
preview = true
select = ['I', 'F', 'E', 'W', 'PL', 'PT']
ignore = [
  "E203",
  "E501",
  "PLC0415",
  "PLR0913",
  "PLR0917",
  "PLR6301", # Method could be static - ignore for flexibility
  "PLR2004", # Magic values - acceptable in many cases
  "PLR0914", # Too many local variables
  "PLR0911", # Too many return statements
  "PLW2901", # Loop variable overwritten - common pattern
]

[tool.ruff.format]
preview = true
quote-style = 'single'

[tool.ruff.lint.per-file-ignores]
"src/activity_log/service.py" = ["PLC0415"]
"src/auth/user_routes.py" = ["PT028"] # Test endpoints in API routes
"tests/**/*.py" = [
  "PLR2004",
  "PLR6301",
] # Allow magic values and non-static methods in tests
"scripts/**/*.py" = [
  "PLR2004",
  "PLR0912",
  "PLR0914",
  "PLR0915",
] # Allow magic values and complexity in scripts

[tool.pytest.ini_options]
pythonpath = "."
addopts = '-p no:warnings'
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.coverage.run]
omit = ["src/teams/*"]
